Day 2 - Conditions (if / else) — JS Practice

This file covers how conditions work in JavaScript.

It uses **if / else** to check if a person is an adult based on age. **else if** is used to assign a grade depending on a score. Two numbers are compared using **comparison operators** to find which is greater. A **boolean** value is checked to show a different message based on whether the user is logged in or not.
